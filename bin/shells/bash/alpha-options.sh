#Moving fast between directories 
alias pd=pushd 
alias pd2='pushd +2'  
alias pd3='pushd +3' 
alias pd4='pushd +4'

# Not working in Mac, but works in Ubuntu
#alias invpn='sudo /etc/init.d/openvpn start'
#alias outvpn='sudo /etc/init.d/openvpn stop'

# Experimental
alias ff="find . -name '*\! {*}*' -ls"
